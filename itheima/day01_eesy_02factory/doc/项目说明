1、使用工厂模式，创建service和dao对象，当删除dao或者service的时候，程序不会报编译错误，只有在运行程序的时候才会报错，
这样就降低了类之间的依赖。需要用到哪里bean就创建那个bean。
2、优化上面的方法，将srvie和dao的bean创建好，存储在一个容器中（map<key,value>）,要用的时候，根据value来获取,容器里面存储
的时单例对象，可复制，比较好。





